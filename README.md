# getting_started_swagger

## Setup project

Install swagger using npm

`$ npm i -g swagger`

Create project using swagger

`$ swagger project create movie-collection`

To start server 

`$ cd movie-collection`

`$ swagger project start`

To edit api in browser using mock

`$ swagger project start -m`

`$ swagger project edit`
